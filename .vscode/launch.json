{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Launch",
            "type": "cppdbg",
            "preLaunchTask": "CMake: build",
            "request": "launch",
            "program": "${workspaceFolder}/build/mesha",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [{"name": "GDK_BACKEND", "value": "wayland"}],
            "externalConsole": false,
            "MIMode": "gdb"
        }
    ]
}